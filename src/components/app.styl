html {
  box-sizing: border-box;
	height: 100%;
}

*, *:before, *:after {
  box-sizing: inherit;
}

body {
	height: 100%;
	font-family: Verdana, Arial, Helvetica, sans-serif;
	margin: 0;
}

.component {
	display: flex;
	flex-direction: column;
	height: 100vh;
	color: #000;
}